// 這個函式會回傳兩個數字相加
function add(a, b) {
  // 把你的解答寫在這裡。要記得 return 喔。
}

// 這個函式會回傳兩個數字相減
// 注意參數的部份是不是有少什麼，補上需要的東西
function sub() {
  // 把你的解答寫在這裡。要記得 return 喔。
}

// 實作 mul 及 div 兩個函式

module.exports = {add, sub, mul, div};
